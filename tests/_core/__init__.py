"""Core functionality tests.

This package contains the most fundamental tests that must pass
for the async-cassandra wrapper to function at all. These tests
run first in the CI pipeline for fast failure detection.
"""
